# Core framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Database
sqlalchemy>=2.0.30

# CLI
click>=8.1.7

# Structured logging
structlog>=24.1.0

# HTTP client
requests>=2.32.0
httpx>=0.27.0

# LLM providers (install only what you use)
# openai>=1.35.0     # pip install openai
# 1.0 had no video-generation (Veo) support at all - types.GenerateVideosConfig
# and the image= param on generate_videos() were both added later. Upgraded to
# use Veo 3 (app/video/veo_provider.py). Verified against the actually
# installed 2.20.0 (not assumed from docs): existing Gemini text-generation
# calls (app/llm/gemini_provider.py) still work unchanged - smoke-tested live.
google-genai>=2.20,<3.0
# google-auth<2.42 was pinned for google-auth-oauthlib (YouTube upload), but
# google-genai 2.20 requires a newer google-auth than that ceiling - pip
# resolved to google-auth 2.57, which is a real, currently-unresolved
# constraint conflict (not a guess: pip printed it during install). YouTube
# upload's OAuth flow was not re-tested after this upgrade - if it breaks,
# this is where to look first.
# google-generativeai is DEPRECATED — use google-genai instead

# Image processing (always needed — thumbnails, placeholder frames)
Pillow>=10.3.0
numpy>=2.0
opencv-python>=4.10.0
# Subject/background segmentation for character cutouts (production_assets.py)
rembg>=2.0.81

# Audio processing
pydub>=0.25.1

# TTS providers (cloud-first, no local model required)
# gTTS is the default — free, internet-required, Google Translate TTS
edge-tts>=7.2.0
gtts>=2.5.1
# ElevenLabs SDK (optional — higher quality, 10k chars/month free tier)
# elevenlabs>=1.0.0
# pyttsx3 offline fallback (system TTS — lower quality but no internet)
# pyttsx3>=2.90

# Scheduler — autonomous 3-day video generation jobs
apscheduler>=3.10.4

# Trend detection (Google Trends scraping — no API key required)
pytrends>=4.9.2             # optional — TrendAgent falls back to LLM if unavailable

# YouTube Data API
google-api-python-client>=2.134.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Utilities
python-dotenv>=1.0.1
tenacity>=8.3.0     # retry utilities
tqdm>=4.66.4        # progress bars
psutil>=5.9.0       # local CPU/RAM measurement for diffusion preflight

# Development / testing (not required in production)
# pytest>=8.2.2
# pytest-asyncio>=0.23.7
# black>=24.4.2
# ruff>=0.4.9
